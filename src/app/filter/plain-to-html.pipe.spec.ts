import { PlainToHtmlPipe } from './plain-to-html.pipe';

describe('PlainToHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new PlainToHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
